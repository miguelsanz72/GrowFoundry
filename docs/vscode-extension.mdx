---
title: "VS Code Extension"
description: "Connect your GrowFoundry projects to AI coding assistants and install MCP with one click."
---

## Overview

The GrowFoundry VS Code extension:

- Logs you in to GrowFoundry (OAuth + PKCE)
- Lets you pick an org/project
- Installs GrowFoundry MCP for your AI client

It launches the MCP installer (`npx @growfoundry/install`) with your project’s API key and API base URL.

## Links

- [VS Code Marketplace](https://marketplace.visualstudio.com/items?itemName=GrowFoundry.growfoundry)
- [Open VSX](https://open-vsx.org/extension/GrowFoundry/growfoundry)

## Install

1. Open VS Code
2. Go to **Extensions**
3. Search for **GrowFoundry**
4. Install the extension published by **growfoundry**

## Use the extension

<Steps>
  <Step title="Open the GrowFoundry panel">
    Click the **GrowFoundry** icon in the Activity Bar (left sidebar).
  </Step>

  <Step title="Login">
    Click **Login with GrowFoundry** and complete the login flow in your browser.

    <Note>The callback is <code>http://127.0.0.1:54321/callback</code>.</Note>
  </Step>

  <Step title="Select a project">
    Pick an organization and project from the GrowFoundry tree view, or run <code>GrowFoundry: Select Project</code> from the Command Palette.
  </Step>

  <Step title="Install MCP">
    Run <code>GrowFoundry: Install MCP</code> (or right-click a project and choose **Install MCP**), then pick the AI client you want to configure.

    The installer runs in a VS Code terminal.
  </Step>
</Steps>

## Supported AI clients

The extension supports the following MCP “clients” (the value passed to `@growfoundry/install`):

| Client | Typical config location |
|---|---|
| `cursor` | `~/.cursor/mcp.json` |
| `claude-code` | `.mcp.json` in your workspace (project-local) |
| `copilot` | `.vscode/mcp.json` in your workspace (project-local) |
| `windsurf` | `~/.codeium/windsurf/` |
| `cline` | Cline VS Code extension config |
| `roocode` | Roo Code VS Code extension config |
| `codex` | OpenAI Codex CLI config |
| `trae` | Trae IDE config |
| `qoder` | Qoder IDE config |

If your client isn’t listed, run the installer manually (see <a href="/quickstart">Quickstart</a>).

## Notes

- **Project-local installs**: `claude-code` and `copilot` require an open workspace folder.
- **Port in use**: free up `54321` and retry login.

