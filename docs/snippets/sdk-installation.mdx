<CodeGroup>
```bash npm
npm install @growfoundry/sdk@latest
```

```bash yarn
yarn add @growfoundry/sdk@latest
```

```bash pnpm
pnpm add @growfoundry/sdk@latest
```
</CodeGroup>

```javascript
import { createClient } from '@growfoundry/sdk';

const growfoundry = createClient({
  baseUrl: 'https://your-app.growfoundry.app',
  anonKey: 'your-anon-key'  // Optional: for public/unauthenticated requests
});
```